// Wait for the DOM to be fully loaded before initializing the game
// Attendre que le DOM soit entièrement chargé avant d'initialiser le jeu
document.addEventListener('DOMContentLoaded', () => {
  // Create the main menu when the page loads
  // Créer le menu principal au chargement de la page
  createMainMenu()
})
